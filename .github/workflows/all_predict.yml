name: Legacy Workflow Placeholder

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain workflow replacement
        run: echo "This legacy workflow is intentionally inert. Use tests.yml and publish.yml."
